int memcmp(const char *str1,const char *str2,size_t n)
{
	const char *s1=str1;
	const char *s2=str2;
	int ret=0;
	assert(str1!=NULL&&str2!=NULL&&n>0);
	while(n-->0)
	{
		if(*s1>*s2)
		{
			ret=1;
			break;
		}
		if(*s2>*s1)
		{
			ret=-1;
			break;
		}
		s1++;
		s2++;
	}
	return ret;
}
